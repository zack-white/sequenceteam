1. Open your local project folder
2. One time: create virtual environment using python3.10
   python3.10 -m venv venv
3. Every time: activate your virtual env
   source venv/bin/activate
4. One time: install package dependencies
   pip install -r requirements.txt